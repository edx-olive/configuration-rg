#!/bin/bash

# This file was generated by Ansible for {{ ansible_fqdn }}
# Do NOT modify this file by hand!

WORKDIR={{ backup_work }}/{{ item[0]['name'] }}

{% for action in item[0]['pre_actions']|default([]) %}
{{ action }}
{% endfor %}

mkdir -p ${WORKDIR}

for redis_data_dir in `redis-cli config get dir | grep -v '^dir$'` ; do
    redis-cli save
    BDIR=`echo ${redis_data_dir} | tr '/' '_'`
    cp -Rap ${redis_data_dir} ${WORKDIR}/
done
